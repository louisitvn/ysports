<div class="container-fluid">
  <div style="margin:20px 0 20px">
    <div class="row">
      <div class="col-md-6">
        <p id="notice"><%= notice %></p>

        <h1>Listing Teams</h1>
        <%= paginate @teams %>
        <table class="display table" cellspacing="0" width="100%">
          <thead>
            <tr>
              <th>Name</th>
              <th>Action</th>
            </tr>
          </thead>

          <tbody>
            <% @teams.each do |team| %>
              <tr>
                <td><%= link_to team.full_name, team.url, :target => "_blank" %></td>
                <td><%= link_to 'Delete', team, method: :delete, data: { confirm: 'Are you sure?' } %></td>
              </tr>
            <% end %>
          </tbody>
        </table>
        <%= paginate @teams %>
      </div>
    </div>
  </div>
</div>
